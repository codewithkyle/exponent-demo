{% extends "_base/page" %}

{% do eagerLoadElements(entry, [
	'homepageHeroCarousel',
	'homepageHeroCarousel:image'
]) %}

{% set data = {
	'heroCarousel': entry.homepageHeroCarousel
} %}

{% block content %}
<article id="homepage" style="display:block;width:100vw;" eager-load-css="homepage">
	{% include "homepage/_hero-carousel" %}
</article>
{% endblock %}
